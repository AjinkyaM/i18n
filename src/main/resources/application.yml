spring:
    datasource:
        url: jdbc:mysql://localhost/jpademo
        username: jpatest
        password: jpapass
        driver-class-name: com.mysql.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: create
        properties:
            hibernate:
                format_sql: true