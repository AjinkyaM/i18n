spring:
    datasource:
        url: jdbc:mysql://localhost/jpademo
        username: jpatest
        password: jpapass
        driver-class-name: com.mysql.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: none
        properties:
            hibernate:
                format_sql: true